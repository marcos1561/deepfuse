Para executar o algoritmo é necessaŕio rodar o arquivo "main.py". Após a execução
será criado uma pasta no local em que o arquivo se encotra contendo as imagens baixadas
e os cutouts. (Por padrão esse pasta se chama "test_region"). 